git is the versoning control system
it used to version the system
by working directory
staging area
local repo
then finaly 
remote repo

commands
working directory to stageing area git add
staging area to local repo git commit -m "name"
local repo to remote repo  1st git remote add name url of remote repo
then git push name master 
it will ask username: give github name
passwrod will generate as token in git hub, take and put it in therminal. your code will push to hub.
inbetween we can user general command
git status, git log, git show, git log --oneline, etc.
